# GridSense-AZ — copy to .env and fill in real values. Never commit .env.

# NREL (NSRDB irradiance + EVI-Pro Lite). Free, instant signup:
#   https://developer.nrel.gov/signup/
NREL_API_KEY=

# EIA (balancing-authority demand, AZPS). Free, instant signup:
#   https://www.eia.gov/opendata/register.php
EIA_API_KEY=

# HuggingFace Hub (weights + Space deploy). Create a WRITE token at:
#   https://huggingface.co/settings/tokens
HF_TOKEN=
HF_USERNAME=dchanda
