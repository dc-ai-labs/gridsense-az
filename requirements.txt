# GridSense-AZ — pinned runtime + training dependencies
# Python 3.11+. Install with: uv pip sync requirements.txt
#
# Versions per PLAN.md §3.3. Keep minimal — transitive deps resolved by uv/pip.

# --- DL / Graph -----------------------------------------------------------
torch>=2.4,<2.6
torch-geometric>=2.6,<2.7
pytorch-lightning>=2.4,<2.6

# --- Power flow -----------------------------------------------------------
opendssdirect.py>=0.9.4,<1.0

# --- Dashboard ------------------------------------------------------------
streamlit>=1.38,<2.0
pydeck>=0.9,<1.0
plotly>=5.23,<6.0

# --- Data wrangling -------------------------------------------------------
pandas>=2.2,<3.0
polars>=1.5,<2.0
duckdb>=1.0,<2.0
pyarrow>=17.0

# --- Geospatial -----------------------------------------------------------
geopandas>=1.0,<2.0
shapely>=2.0,<3.0

# --- Explainability -------------------------------------------------------
captum>=0.7,<1.0

# --- HF Hub / datasets ----------------------------------------------------
huggingface_hub>=0.24
datasets>=2.21

# --- Utilities ------------------------------------------------------------
requests>=2.32
pyyaml>=6.0
numpy>=1.26,<2.2
scipy>=1.13
scikit-learn>=1.5

# --- Test (available at runtime for CI) -----------------------------------
pytest>=8.3
