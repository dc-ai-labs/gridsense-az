# Dev-only tooling. Install after requirements.txt:
#   uv pip install -r requirements-dev.txt

-r requirements.txt

black>=24.8
ruff>=0.6
pytest>=8.3
pytest-xdist>=3.6
ipykernel>=6.29
jupyterlab>=4.2
