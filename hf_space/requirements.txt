# HuggingFace Space — runtime-only dependencies for the dashboard.
# Deliberately slim: no training / geospatial / dataset-bench deps.
# Build target: Streamlit SDK on CPU Basic (16 GB).

torch>=2.4,<2.6
streamlit>=1.38,<2.0
pydeck>=0.9,<1.0
plotly>=5.23,<6.0
pandas>=2.2,<3.0
duckdb>=1.0,<2.0
opendssdirect.py>=0.9.4,<1.0
huggingface_hub>=0.24
captum>=0.7,<1.0
numpy>=1.26,<2.2
pyyaml>=6.0
